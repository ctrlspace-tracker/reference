## Angular reference (cheatsheet)

### Setup
if npm is not already install, then install node/npm in your local machine. 
```shell script
brew install node
```
Install Angular CLI
```shell script
npm install -g @angular/cli
```


